[package]
name = "process-context"
version = "0.1.0"
edition = "2021"

[lib]
name = "process_context"
path = "src/lib.rs"

[dependencies]
libc = "0.2"
tracing = "0.1"

[dev-dependencies]
# For testing
