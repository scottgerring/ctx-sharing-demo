{
	"name": "Rust (Ubuntu 24.04, amd64)",
	"image": "mcr.microsoft.com/devcontainers/base:ubuntu-24.04",

	"platform": "linux/amd64",

	"features": {
		"ghcr.io/devcontainers/features/rust:1": {}
	},

	"runArgs": [
		"--privileged",
		"--cap-add=CAP_BPF"
	],

	"postCreateCommand": "sudo apt-get update && sudo apt-get install -y libclang-dev llvm-dev librust-openssl-dev openjdk-21-jdk dwarves musl-dev musl-tools && ./prepare-rust.sh",

	"customizations": {
		"jetbrains": {
			"backend": "RustRover"
		}
	}
}
